# Push instructions

This repository is ready locally. To upload it, authenticate GitHub for this WSL environment, then run:

```bash
cd /home/jeongwon/.openclaw/workspace/Prusa-Enclosure
git push -u origin main
```

Current SSH public key for this machine:

```text
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMTZoi7umca6T0Hs7ulT65z94VPdAAhsJ0zl3tnYP4tL jeongwonclaw@PC
```

Add it to GitHub: Settings → SSH and GPG keys → New SSH key.
